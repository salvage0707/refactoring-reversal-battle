version: "3.8"

services:
  ruby:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - bundle:/usr/local/bundle
    working_dir: /app
    stdin_open: true
    tty: true
    environment:
      - BUNDLE_PATH=/usr/local/bundle

volumes:
  bundle:
